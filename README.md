# Clean + Riverpod Template (V2)

This folder contains deliverables for the new company template strategy:

1. `fundex` (directory: `fundex`): business app repository scaffold (Clean Architecture + Riverpod).
2. `mobile_core_sdk`: reusable core plugin repository scaffold (independent Git + versioned packages).
3. `core_network` token refresh interceptor draft with tests.

The scaffolds are intentionally minimal but runnable as a starting point for migration from the legacy GetX codebase.

## Tooling baseline

- Flutter: `3.41.4`
- Dart: `3.11.0`
- Version manager: `fvm`

Use `fvm` prefix for all Flutter/Dart commands in both repos.

## Long-running project workflow

- Codex/task rules: `AGENTS.md`
- AI project control memory: `docs/ai-project-control.md`
- Hotel booking roadmap: `docs/hotel-booking-roadmap.md`
- Release/CI flow: `docs/github-actions-release-flow.md`
